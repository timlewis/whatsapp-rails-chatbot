<div class="space-y-6">
  <!-- Page Header -->
  <div>
    <h1 class="text-1 text-gray-900">Edit <%= @persona.name %></h1>
    <p class="text-3 text-gray-600 mt-2">Modify the AI personality settings</p>
  </div>

  <!-- Form -->
  <%= render "form", persona: @persona %>
</div>
